version: '3.5'

services:
  mail-postfixadmin:
    image: hardware/postfixadmin:${IMAGE_TAG:-latest}
    domainname: ${DOMAIN_NAME}
    #hostname: mail
    networks:
      - mail-net
    environment:
      - DBHOST
      - DBPASS

networks:
  mail-net:
    external: true
