version: '3.5'

services:
  minio:
    image: minio/minio:${IMAGE_TAG:-latest}
    command: server /export
    environment:
      - MINIO_ACCESS_KEY
      - MINIO_SECRET_KEY
    networks:
      - gitlab-net
      - traefik-net
    volumes:
      - export-vol:/export
      - data-vol:/data

networks:
  gitlab-net:
    external: true

  traefik-net:
    external: true
